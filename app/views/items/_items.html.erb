<ul>
  <% items.each do |item| %>
    <p>
      <%= link_to item.title, item_path(item) %> | $<%= item.price.to_f/100 %> |
        <%= button_to 'Add to Cart', line_items_path(item_id: item) if
          current_user %>
    </p>
  <% end %>
</ul>
